# Silqueen Instagram Automation Rules

- **CRITICAL**: The application is currently under Meta App Review (submitted July 7, 2026).
- **DO NOT TOUCH CORE FUNCTIONALITY**: Never modify or alter the webhook endpoint (`app/api/webhook/route.ts`), verification logic, Vercel environment variables, or database table structures (`instagram_accounts`, `automation_logs`, `automation_flows`) during the review stage.
- **ACTIVE FLOWS**: Keep the default "PRICE" and "GET" trigger flows intact and fully operational so that Meta reviewers can successfully trigger and test them live.
